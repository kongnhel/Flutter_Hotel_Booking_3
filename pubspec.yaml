name: hotel_booking
description: "A new Flutter project."

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none"

# The following defines the version and build number for your application.
version: 1.0.0+1

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  cached_network_image: ^3.4.1
  badges: ^3.1.2
  carousel_slider: ^5.1.1
  flutter_image_stack: ^0.0.7
  flutter_svg: ^2.2.0
  http: ^1.4.0
  shared_preferences: ^2.5.3
  flutter_admin_scaffold: ^1.4.0
  firebase_core: ^3.15.1
  firebase_auth: ^5.6.2
  image_picker: ^1.1.2
  path: ^1.9.1
  firebase_storage: ^12.4.9
  image_picker_for_web: ^3.0.6
  firebase_storage_web: ^3.10.16
  cloudinary_flutter: ^1.3.0
  cloudinary_public: ^0.23.1
  flutter_launcher_icons: ^0.14.4
  intl: ^0.20.2
  flutter_image_compress: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices.
  flutter_lints: ^5.0.0

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icons/
    - assets/images/

  # To add custom fonts to your application, add a fonts section here.
  # fonts:
  #   - family: NotoSansKhmer
  #     fonts:
  #       - asset: assets/fonts/NotoSansKhmer-Regular.ttf # Ensure this file exists at this path!

  #   - family: NotoColorEmoji
  #     fonts:
  #       - asset: assets/fonts/NotoColorEmoji.ttf # Ensure this file exists at this path!
